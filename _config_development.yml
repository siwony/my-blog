# Development environment configuration
# This file overrides _config.yml for development environment

# Disable compression in development
assets:
  compression: false
  source_maps: true
  plugins:
    js: []
    css: []

# Development specific settings
url: "http://localhost:4000"
show_drafts: true
incremental: true

# Enable livereload for development
livereload: true